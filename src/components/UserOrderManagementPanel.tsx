"use client";

export default function UserOrderManagementPanel({
  pair,
  type,
}: {
  pair: string;
  type: string;
}) {
  return (
    <div className="bg-[#181A20] rounded-[10px] min-h-[400px] mb-[30px] m-1 text-white">
      User Order Management Panel
    </div>
  );
}
